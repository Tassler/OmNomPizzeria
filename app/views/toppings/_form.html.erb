<%= form_for(@topping) do |f| %>
  <% if @topping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topping.errors.count, "error") %> prohibited this topping from being saved:</h2>

      <ul>
      <% @topping.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
